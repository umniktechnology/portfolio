# CDN (classic) is retired — Azure no longer accepts new profile creation.
# The portfolio is served directly from Azure Storage static website endpoints.
# This is free-tier eligible and sufficient for a personal portfolio.
# If you later need a CDN, use Azure Front Door Standard (azurerm_cdn_frontdoor_profile).
